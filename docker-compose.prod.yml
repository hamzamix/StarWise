services:
  starwise:
    build: .
    container_name: starwise_app
    ports:
      - "4053:4000"
    volumes:
      - starwise_db:/app/backend
    restart: unless-stopped
    # For production, use environment variables from your deployment platform
    environment:
      - GITHUB_CLIENT_ID=${GITHUB_CLIENT_ID}
      - GITHUB_CLIENT_SECRET=${GITHUB_CLIENT_SECRET}
      - GITHUB_CALLBACK_URL=${GITHUB_CALLBACK_URL:-http://192.168.1.30:4053/auth/github/callback}
      - SESSION_SECRET=${SESSION_SECRET}
      - API_KEY=${API_KEY}
      - PORT=${PORT:-4000}

volumes:

  starwise_db:



